lets sett up this repo to setup a0 agent-zero https://github.com/cbwinslow/agent-zero lets get this setup on cloudflare. or lets setup the docker instance to use cloudfalre tunnels so it can be accessed anywhere using cloudflare tunnels. lets read the documentation for agent-zero and also cloudflare and tunnels and lets formulate a plan to do so.there should be, in the agent zero documentation a youtube video i think that has instructions as well. please come up with a plan to do this. write scripts and markdown files wth instructions. generalize the scripts so others can use them as well. add features as well. please add features as well related to the cloudflare setup. lets consider using agents feauture from cloudflare and all of the other features from cloudflare. lets make a 
